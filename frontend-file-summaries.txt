{
  "frontend/src/components/common/SearchBar.tsx": "Sticky search bar with autocomplete suggestions updated to use `product.image` and `product.unit` from the new Product interface instead of the old `emoji` and `weightOrQuantity` fields.",
  "frontend/src/App.tsx": "Main app component with page-stack navigation; now imports FirstVisitModal and shows it as an overlay when the user is not logged in (checked after a 100ms rehydration delay), hiding it automatically once the user logs in or skips.",
  "frontend/src/pages/OrderTracking.tsx": "Live order tracking page with animated 5-step status timeline, rider info, ETA countdown, and delivery progress bar. Toast notification messages updated to be more customer-friendly: \"Being Prepared\" now shows \"Your order is being prepared! 🍳 Hang tight!\", \"Picked Up by Rider\" shows \"Your order has been picked up! 🛵 Rider is on the way!\", \"Out for Delivery\" shows \"Almost there! Your order is out for delivery 📦\", and \"Delivered\" shows \"Order delivered! Enjoy your items 🎉\".",
  "frontend/src/pages/Profile.tsx": "User profile page that shows a login prompt card (with benefits list) when unauthenticated, and the full profile (addresses, orders, loyalty, language, support) plus a Logout button when logged in.",
  "frontend/src/pages/admin/AdminPanel.tsx": "Admin panel with dashboard, product management, orders, and analytics tabs. Updated to use new Product interface fields (`inStock`, `image`, `unit`, `reviews`) and updated category revenue chart to reflect the three new categories (Vegetables, Eggs, Meat).",
  "frontend/src/pages/PaymentSuccess.tsx": "Simple payment success confirmation screen with green checkmark and continue shopping button.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure screen with error icon, retry button, and back to cart option.",
  "frontend/src/lib/authStore.ts": "Zustand store with localStorage persistence (key: `murkimart-auth`) managing customer authentication state (isLoggedIn, user object with name/phone/email), exposing login() and logout() actions via the useAuthStore hook. The phone field is now required in AuthUser.",
  "frontend/src/pages/Login.tsx": "Customer login page with Phone/OTP and Email/Password tabs, inline validation errors, mock OTP flow, and a \"Continue as Guest\" option; on success calls useAuthStore.login() and navigates to Home. Fixed the email login path to pass `phone: ''` to satisfy the updated AuthUser type requiring phone as a required string field.",
  "frontend/src/pages/Checkout.tsx": "Checkout page with delivery address selection, Cash on Delivery payment, and an expanded Bill Details section that now lists each item with its weight/unit descriptor (e.g., \"500g\", \"1kg\", \"12 pieces\") and quantity before the totals summary. The WhatsApp order message also includes the weight/unit for each line item.",
  "frontend/src/lib/products.ts": "Product catalog with CATEGORIES array now filtered to only three entries — Vegetables (fruits-vegetables), Eggs (eggs), and Meat (meat). Added dedicated egg products (eg-001 to eg-005) under the new `eggs` category and meat/seafood products (mt-001 to mt-008) under the new `meat` category; all other categories remain in ALL_PRODUCTS for internal use but are not shown in navigation. TOP_ESSENTIALS updated to feature vegetables, eggs, and meat items.",
  "frontend/src/pages/ProductCatalog.tsx": "Product catalog page with category tabs and filters. Removed the `vegOnly` filter (since `isVeg` no longer exists on Product) and updated `isInStock` references to `inStock`.",
  "frontend/src/components/home/CustomerCareContact.tsx": "Orange gradient banner for customer care with the phone number (+91 73480 50803) now displayed as the first element at the top, followed by the support header row (icon, label, live badge), the \"Tap to Call & Order\" CTA button, and availability sub-text — all linking to tel:+917348050803.",
  "frontend/src/pages/Home.tsx": "Home page layout that now includes the new ProfileSummary component placed below the CustomerCareContact banner and FlashDeals section, with a divider separator above it.",
  "frontend/src/components/catalog/ProductCard.tsx": "Product card component importing Product from @/lib/products; displays image, name, unit, rating, price with discount badge, Add/quantity-stepper cart button, and a new orange \"Buy Now\" button (Zap icon) that triggers instant order placement for in-stock products only.",
  "frontend/src/components/home/FlashDeals.tsx": "Flash deals section updated to compute discount percentage from `price` vs `discountedPrice` fields instead of the removed `discountPercent` field.",
  "frontend/src/lib/cartStore.ts": "Zustand cart store where `getDeliveryFee()` now always returns 0 (free delivery), and `getTotal()` is updated to exclude any delivery fee from the calculation.",
  "frontend/src/pages/Cart.tsx": "Cart page updated to always show \"FREE 🎉\" for delivery fee and replaced the conditional \"add ₹X more for free delivery\" banner with a permanent \"FREE delivery on all orders!\" banner.",
  "frontend/src/lib/addressStore.ts": "Zustand address store with Zustand persist middleware (key: `murkimart-addresses`) that saves and rehydrates addresses and selectedAddressId from localStorage. Seed addresses removed so persisted data is used on reload; addAddress, deleteAddress, and setSelectedAddress all trigger persistence.",
  "frontend/src/lib/orderStore.ts": "Zustand order store restored to the original Order interface (with address string, savings, riderName, estimatedMinutes, Date createdAt); createOrder accepts the full order data minus auto-generated fields and sets the new order as currentOrderId.",
  "frontend/src/lib/navigationStore.ts": "Lightweight Zustand store holding a `navigateTo` function reference so hooks outside the React tree (like useInstantOrder) can trigger page navigation.",
  "frontend/src/hooks/useInstantOrder.ts": "Instant order hook updated so delivery fee is always 0 (free), total equals subtotal, and the WhatsApp message shows \"Delivery: FREE 🎉\" instead of a calculated fee.",
  "frontend/src/components/onboarding/FirstVisitModal.tsx": "First-visit onboarding modal that collects a 10-digit phone number and delivery address (house number, street, locality dropdown for Murki Jaunpur areas, landmark). On submit it calls authStore.login and addressStore.addAddress, then closes; includes a subtle \"Skip for now\" option that closes without saving.",
  "frontend/src/components/home/ProfileSummary.tsx": "New profile/account summary section component for the Home page that shows a logged-in user's name and phone in an orange gradient card (or a green \"Login / Sign Up\" prompt when unauthenticated), a 2×2 grid of quick-link cards (My Orders, Saved Addresses, Loyalty Points, Help & Support), and a \"Go to Profile\" button — all navigating to the Profile page via navigationStore."
}